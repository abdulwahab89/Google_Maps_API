import 'package:flutter/material.dart';

class CovertLatlingToAddress extends StatefulWidget {
  const CovertLatlingToAddress({Key? key}) : super(key: key);

  @override
  State<CovertLatlingToAddress> createState() => _CovertLatlingToAddressState();
}

class _CovertLatlingToAddressState extends State<CovertLatlingToAddress> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Convert to address'),
      ),
      body: const Column(
        children: [],
      ),
    );
  }
}
